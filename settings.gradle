pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()

        // 카카오 sdk 레포지토리 설정
        maven { url 'https://devrepo.kakao.com/nexus/content/group/public/' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "23_HF069"
include ':app'
